## Docker neutralizado
# O projeto não vai usar Docker. Arquivo mantido somente como nota.
# Este arquivo foi atualizado para conter uma instrução mínima `FROM`
# para evitar erros de "file with no instructions" em pipelines.
FROM scratch
# Para restaurar o Dockerfile original, recupere-o do histórico Git.
